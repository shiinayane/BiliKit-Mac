# macOS metadata
.DS_Store

# Xcode local state and build products
DerivedData/
*.xcresult/
*.xcuserstate
**/*.xcodeproj/xcuserdata/
**/*.xcworkspace/xcuserdata/

# Swift Package Manager local state and build products
.build/
.swiftpm/

# Local environment and secrets; a documented template may be tracked.
.env
.env.*
!.env.example

# Complete third-party or research checkouts. These are neither project source
# nor redistribution inputs, and the directory is intentionally absent from the
# Xcode project. See docs/README.md for the policy.
/references/
