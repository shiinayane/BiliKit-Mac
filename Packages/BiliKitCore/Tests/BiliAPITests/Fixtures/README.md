# BiliAPI 测试 fixture

本目录中的 JSON 均为手写、脱敏的最小 contract fixture：

- 标识符、标题、作者和统计数据为虚构值。
- 图片与媒体地址只使用 `example.invalid`。
- 不包含 Cookie、token、WBI 签名、实时响应 body 或带签名媒体 URL。
- 字段形状依据 2026-07-21 的匿名接口响应重新核对，但内容不是线上响应副本。
