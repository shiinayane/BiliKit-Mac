# M4.4 决策治理修订验证（2026-07-23）

## 范围

本轮只修订治理规则、Reviewer mandate、PR 模板和 M4.4 文档；没有实现或运行 renderer、runner、schema、watchdog 或 benchmark harness。

旧 M4.4 长契约的确认已撤销，并缩成不可授权的历史 stub。用户随后将临时负载选择交由工程判断，要求增加日文、韩文覆盖，并于 2026-07-23 在 Codex task `019f89a8-67ed-73e1-9915-5d7ca36ccd1e` 的连续入站回复中明确同意 D3。对 A→B 条件的唯一重大误解在解释后已纠正；T3 技术细节由独立技术审查负责，不要求用户逐项掌握。该授权覆盖 Phase 0 A 与满足既定 Gate 后的条件性 Phase 1 B，不覆盖 Phase 2 或生产 renderer。

## 独立审查

使用互不继承结论的独立只读上下文，依次完成：

- 价值与简化：检查行动决策、候选顺序、投入产出和 harness 预算；最终 blocker 复核为 resolved。
- 可理解性：检查非专家复述、revision 绑定和 Agent 不得代确认；最终 blocker 复核为 resolved。
- 红区技术：检查确认闭环、旧 generation completion、MainActor owner、输入偏置与 bitmap backing 安全上限；最终 blocker 复核为 resolved。

D3/T3 的定向复审进一步确认：20/40/80 的等间隔负载与并发推导、五类文字 fixture、A→B 唯一入口、共享文字测量/layer shadow、B 的 MainActor 同步栅格化和 128 MiB backing 安全 cap 均无剩余 blocker。

用户随后指出原可理解性 Gate 已过度演化为闭卷复述。该“回答不完整即失败”的结论已撤销；Gate 改为不超过 300 字摘要、最多三个授权问题，并只由明确的重大误解阻断。次要遗漏允许确认，技术实施细节交由技术审查。用户此前对 D3 的明确同意继续有效，无需重新考试。

审查明确拒绝恢复 broker、PGID、schema/hash、全矩阵、通用 watchdog 或首轮 30 分钟 soak。

## 验证

环境：macOS，Apple Silicon，Asia/Tokyo。

```sh
sh Scripts/run-quality-gates.sh static
```

结果：通过。架构、秘密、工程/entitlement/最低系统版本静态契约均通过，`git diff --check` 无错误。

## 未验证边界

- static Gate 不证明 renderer 性能、生命周期或真实播放 surface。
- D3 已确认，T3 独立技术审查无 blocker；但须先合入本治理热修，之后只能在新的干净 worktree 开始 Phase 0 A 和条件性 Phase 1 B，不能恢复旧 harness，也不能进入 Phase 2。
- 当前 Mac 的实验结果以后也不能外推为最低支持设备或产品上限。
