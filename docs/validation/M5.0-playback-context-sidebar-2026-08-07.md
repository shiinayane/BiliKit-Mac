# M5.0 播放工作台上下文侧栏验证（2026-08-07）

## 状态

Stage 1.1 与 Stage 1.2 的生产实现、自动化验证和独立代码审查已经完成。Stage 1.3
的自动化部分通过；真实开发版已准备给用户执行窗口、键盘和辅助功能人工复核。

用户已在 2026-08-07 确认基本布局迁移可以接受，包括详情／分 P 的 sidebar 信息架构；
本次确认不等同于 VoiceOver、Full Keyboard Access 或真实远端播放能力已经完整通过。

在用户确认人工检查结果前，本记录不把播放工作台上下文侧栏标记为完整完成，也不据此
启动分 P 切换、评论或相关推荐实现。

## 被验证的生产结果

- 播放状态继续使用同一个系统 `NavigationSplitView` sidebar；普通来源导航与播放上下文
  不会同时占用两个侧栏。
- sidebar 读取 `GuestVideoViewModel.presentedContext`，依次呈现非空真实简介、多分 P
  只读目录和评论 unavailable state。
- 单分 P 隐藏目录，空简介隐藏简介模块；只读分 P 行没有 Button/action 语义，当前项仅以
  视觉状态和 `isSelected` trait 表达。
- detail 不再重复简介、分 P 或旧 400 pt rail；`playerContent()` 保持一个调用点。
- loading／failed 替换状态阻止旧 sidebar 内容交互并将其从辅助阅读路径隐藏，状态 owner、
  generation、取消和 reset 仍由既有 ViewModel／Coordinator 生命周期管理。
- 评论和相关推荐没有新增 endpoint、模型、Repository、占位 View 或生产 fixture。

## 自动化证据

验证环境为 Xcode 26.6（17F113）、macOS 26.6.1 arm64，开发产品 Bundle Identifier 仍为
`com.shiinayane.BiliKitMac.dev`。

### 最高仓库门禁

`sh Scripts/run-quality-gates.sh app` 通过：

- 静态架构、秘密模式、工程／entitlement／最低系统版本和 Swift 格式契约通过；
- Swift Package build 与 tests 通过；
- App build-for-testing 与 App unit tests 通过；
- signed Keychain smoke 在无显式 opt-in 的普通 App gate 中按契约跳过，不能由此声称真实
  Keychain 检查通过。

### 签名 XCUI

两条显式 Debug／UITest 路径在签名开发版上通过，共 2 tests、0 failures、0 skips：

1. `testPlaybackRoundTripInOneWindow`
   - 生产 `PlaybackContextSidebar` 出现，简介可通过 macOS 原生 disclosure 折叠；
   - 3 个真实 fixture 分 P 出现，只有 P1 带 selected trait，P1／P3 均不暴露 Button；
   - 评论 unavailable state 存在，detail 中旧 rail 和旧分 P 行不存在；
   - 系统 sidebar 隐藏后 disclosure 不可交互且 sidebar 无键盘焦点，恢复后重新可交互；
   - 窗口从 1320×820 调整到 1080×680 后，本地真实 `AVPlayerItem` 的 player identity、
     item identity 和 generation 不变，时间线继续推进；
   - 系统 Back 返回原来源，播放项、observer 和临时媒体资源按既有生命周期停止清理。
2. `testPlaybackSidebarHidesEmptySummaryAndSinglePart`
   - 空简介不创建简介模块；
   - 单分 P 不创建目录或 P1 行；
   - 评论 unavailable state 仍存在；Back 后本地播放器停止。

这些 fixture 仅在 `#if DEBUG` 且显式 `-ui-testing` 参数下启用，不进入生产能力或数据声明。

### Player host 与 A → B 证据归因

- `PlayerHostViewIdentityTests` 直接遍历唯一真实 `AVPlayerView`，覆盖 item update、resize、
  removal 和 dismantle identity。
- `VideoDetailLifecycleTests` 通过生产 Coordinator／ViewModel 生命周期覆盖 A → B loading、
  content failure、retry、success 和 Back，并直接检查真实 `AVPlayerView` identity。
- production sidebar 的签名 XCUI 另外覆盖 disclosure、系统 sidebar 和真实窗口 resize 下的
  AVPlayer／item／generation／时间线连续性。

因此没有为测试向 production AX 树增加 AVPlayerView identity 探针。上述证据共同支持稳定
host，但 XCUI 本身不声称直接读取了 `AVPlayerView` 的 `ObjectIdentifier`。

## 独立审查

独立上下文 reviewer 首轮没有发现 P0／P1，提出 3 个 P2 证据缺口：单分 P／空简介／只读
语义、production sidebar resize 连续性，以及 sidebar 隐藏／恢复断言强度。补齐上述测试后，
定点复核结论为无 P0–P3、无需新增 production player identity 探针。

## 用户人工复核清单

请在本次启动的签名开发版中使用真实游客数据检查：

1. 从热门或搜索打开一个多分 P 且简介较长的视频。
2. 确认同一个系统 sidebar 原位变成“简介—分 P—评论尚未接入”，detail 没有重复简介或分 P。
3. 展开／折叠简介和分 P，选择简介文本；播放器画面和时间线不应重置。
4. 用系统 toolbar 隐藏／显示 sidebar，再把窗口从宽布局拖到约 1080×680；播放器仍可观看，
   sidebar 可以独立滚动，内容不覆盖播放器。
5. 使用系统 Back，确认返回原来源及原有草稿／工作集位置。
6. 开启 VoiceOver，检查阅读顺序、disclosure 展开状态、当前分 P selected 播报，以及评论
   unavailable 文案；loading／failure 时不应朗读上一视频的简介或分 P。
7. 开启 Full Keyboard Access，检查 sidebar toggle、Back、简介／分 P disclosure 和播放器
   控制之间的焦点顺序，没有隐藏焦点或焦点陷阱。
8. 若系统启用了 Reduce Motion 或较大文字，再复核折叠、sidebar 显隐和长文本滚动不会造成
   内容丢失或播放器重建。

## 尚未关闭的边界

- 人工 VoiceOver、Full Keyboard Access、Reduce Motion 和较大文字结果等待用户确认；
- 真实远端简介／分 P／DASH-CDN 行为需要本次用户运行观察，自动 fixture 不替代该结论；
- 860×620 不是本阶段验收尺寸，旧 test-only `setContentSize` 也不证明用户可达；
- Intel、其他 macOS 版本和远端 CI 未由本地验证覆盖；
- 分 P 点击、评论能力和横向相关推荐仍属于后续独立阶段。
