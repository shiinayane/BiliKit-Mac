# 产品候选登记

> 这里只保存已经确认值得继续处理、但尚未成为当前实施阶段的用户结果。条目没有优先级、版本承诺
> 或实现授权；开始前必须重新核对当前代码、平台行为、外部接口、隐私与成本。

| 候选 | 期望的用户结果 | 进入实施前的边界 |
| --- | --- | --- |
| 播放器区域纵向滚动 | 指针位于视频上方时，触控板纵向滑动仍能自然滚动详情页 | 保持系统控制条、全屏、横向手势和辅助功能；先用真实输入复现冲突 |
| 播放器浮动 action | 为确有必要的 BiliKit 特有播放操作提供易发现入口 | 先确定具体 action、位置、显隐和反馈；不以 `controlsStyle` 或已有浮层证明需求 |
| 弹幕更小字号 | 在高分辨率大屏上提供更密集但仍清晰的弹幕显示 | 先裁决可读性、碰撞 lane、缩放、VoiceOver 和真实性能，不只降低常量 |

V1 Developer ID 发布准备已经进入 `ROADMAP.md`，首页推荐、相关推荐、只读评论和服务端观看进度已经实现，不再作为
候选重复登记。Sparkle 属发布安全阶段，已接入；Cloudflare 已限于签名静态元数据，其他用途仍须
再立项。
