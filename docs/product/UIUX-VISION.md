# BiliKit App UI/UX 产品蓝图

> 状态：长期产品方向，记录已讨论的信息架构与后续机会，不代表对应服务端能力、持久化 schema 或写操作已经授权。具体能力仍需进入路线图并通过独立契约与 Gate。

## 目标

BiliKit 应成为适合日常使用的原生 Mac 客户端，而不是把网页或当前三栏原型逐项搬进 SwiftUI。界面优先遵循 Mac 的导航、窗口、键盘和辅助功能习惯，同时保留 B 站用户熟悉的视频卡片、播放信息层级和内容语义。

产品形态按“搜索／热门／Library 等一级入口 → 内容页面 → 同窗口独立播放页”组织。同一个
系统侧栏在普通状态承担稳定导航，在播放状态承担当前视频的阅读上下文；短任务使用 sheet；
页面不能依赖假内容、空壳模块或未来接口才能成立。

## 全局导航

- 使用“侧栏 + 主内容”的两栏外壳，搜索位于侧栏最上方，热门与观看历史位于其下，账户入口固定在左下。
- 进入播放页后不创建第二个侧栏：同一系统 sidebar 从来源导航切换为视频上下文，系统返回
  后恢复原来源导航及其工作集、搜索草稿与滚动位置。
- 搜索输入框位于搜索页的主内容 toolbar，不嵌进侧栏。
- 热门、搜索、观看历史和未来个性推荐首页复用响应式 16:9 网格与共享卡片语言，但各
  页面保留自己的内容语义、加载、空、错误、分页和恢复边界；个性推荐不能由热门数据
  冒充。
- 内容首载优先评估使用与最终布局相符的骨架屏，而不是让居中转圈成为主要页面结构。
  首载、刷新、追加、页面切换与失败恢复应形成一致、克制的过渡语言；动画不得改变
  稳定 item identity、滚动位置或播放器 host。具体样式、时长和降级方式随对应页面
  纵向切片决定。
- 打开视频进入同一窗口的独立播放页；返回时恢复来源页面、滚动位置和选中卡片。

## 搜索页长期形态

### 有搜索结果时

- 结果直接使用与热门页相同的视频卡片流。
- 分类 Tab、筛选、排序和更多结果类型属于独立搜索功能，不因 UI 精修提前加入。

### 尚未输入关键词时

空页应提供有意义的下一步，而不是永久留出大片空白。候选内容按以下优先级逐步评估：

1. **本机搜索历史**：帮助重复访问；必须支持单项删除与全部清空，并明确只保存在本机。
2. **Bilibili 热搜**：帮助发现当前话题；需要确认稳定的服务端来源、失败语义与匿名／登录边界。
3. **发现内容**：可评估复用现有热门视频，或在服务端能力明确后增加分区入口；不能为了填充版面伪造推荐语义。

这些模块不是必须同时出现。每次只接入能证明用户价值且具有真实数据闭环的最小模块；若内容重复、过时或使搜索意图变模糊，应保持更简单的空状态。

### 搜索历史的数据边界

搜索词可能暴露兴趣、健康、身份或其他敏感意图，不能作为普通界面偏好写入 `UserDefaults`。若立项，应在本地持久化纵向切片中确定：

- 由 Application port 隔离具体存储，Feature 不直接 import SwiftData；
- 容量、保留期限、去重与排序规则；
- 单项删除、全部清空、登出和卸载边界；
- 不保存账号凭据、远端响应正文或不必要的用户身份；
- 是否默认启用及如何向用户说明“仅保存在本机”。

具体使用 SwiftData、文件或其他 adapter，应由持久化契约根据查询和迁移需求决定，不在产品蓝图中预选实现。

## 播放页长期形态

- detail 主区顺序为“标题—UP 主／播放量／弹幕数—带系统字幕菜单的播放器—弹幕设置—未来横向相关
  推荐”。播放器及其控制始终是稳定主锚点。
- 播放状态下，同一系统 sidebar 顺序为“真实简介—真实分 P—评论阅读上下文”。简介可折叠
  且可选择文本；单分 P 隐藏整个目录，多分 P 使用有界、可独立滚动的 `List` 显示当前项
  反馈，不让长目录挤出其余观看上下文。长目录以“分 P（总数）”和系统滚动行为提示后续
  内容，保持最多 5 个完整、随文字尺寸缩放的行，不故意裁出半个下一行；折叠后重新展开
  时自动定位当前分 P，不要求用户从 P1 重新寻找。
- 多分 P 目录已经以新的 CID playback identity 接通真实切换；请求中、已呈现、失败与重试
  状态必须诚实区分，并同步取消和替换媒体、字幕与弹幕会话。
- sidebar 在简介和播放选择后显示匿名只读评论，覆盖热门／最新、主评论增量分页和楼中楼分页；
  缺少 AID、空结果、受限、失败和本次 1,000 条上限均显示诚实状态，不展示合成评论或登录诱导。
- 相关推荐已经确定为 detail 下方的单排横向 shelf：使用 16:9 卡片、两行标题、清晰的键盘
  focus 和下一张露出，不自动播放、不无限循环。它可以突破正文最大宽度铺满 detail 列，
  但不能跨过 `NavigationSplitView` 分隔线、覆盖 sidebar 或创建第二个 player host。
- 在可靠推荐数据来源、模型、取消和失败契约建立前，不显示推荐标题、空架或合成内容。
- 窄窗口优先保证播放器可观看宽度，sidebar 可以通过系统行为收起；布局变化不得复制或替换
  播放器 host。
- 触控板双指纵向滑动在鼠标位于播放器上方时也应继续滚动页面，不得被 `AVPlayerView` 解释为时间 seek；横向手势是否保留快进／后退应在独立交互收口中决定，并确保不干扰播放器控制条、全屏和辅助功能。

播放工作台利用大窗口、系统分栏与稳定播放器 host 形成“主区持续观看、sidebar 阅读
上下文”的结构。“一边观看、一边浏览评论”不应只是把网页评论接到播放器下方；未来真实
评论能力仍需保持可隐藏、可调宽且独立滚动：

- 播放器是稳定主锚点；评论区展开、收起、缩放、分页或失败不得重建播放器、
  中断时间轴或改变字幕／弹幕会话。
- 评论滚动、文本选择与输入拥有独立焦点和手势边界，不抢占播放控制、seek、
  全屏或全局媒体快捷键，也不让页面因新评论自动跳动。
- 宽窗口可以并排阅读，窄窗口和全屏优先保证观看，评论退化为用户主动打开的
  辅助区域；是否记忆宽度与展开状态需由后续本地状态契约决定。
- 评论写操作、真实评论图片和更广泛的外链／成员页导航继续单独立项；匿名读取、分页与排序已按
  单一 AppKit scroll owner 接入，不与播放器、字幕或弹幕共享生命周期。

UP 主扩展资料、关注、评论和相关推荐分别涉及新数据或写操作，必须独立立项。确认未来位置
不等于授权接口、状态或占位实现；当前阶段只使用已有 pages 与 playback repository 接通
真实简介和分 P，不授权上述相邻能力。

## 演进规则

- 新 endpoint、本地内容历史、推荐语义、关注及其他写操作必须在成为唯一下一阶段后
  单独建立契约。
- 每个新增模块先回答“它解决什么空白、失败时页面是否仍成立、是否值得增加状态与清理成本”。
- 本蓝图记录方向；`docs/ROADMAP.md` 只记录唯一已选择的后续阶段，候选登记防止未排期
  事项丢失，决策契约决定当次授权，验证记录才是完成证据。
