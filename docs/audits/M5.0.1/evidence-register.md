# 外部事实证据登记表

状态：十条审计线的可复用外部事实已登记；由主 Agent 单写。

## 字段

| Fact ID | 主张 | 类型 | 来源与版本 | 日期 | 条件／反例 | 强度 |
| --- | --- | --- | --- | --- | --- | --- |
| API-F001 | 字幕目录的当前独立实现共同使用 `/x/player/wbi/v2` | OSS 实现 | wiliwili `88e5876`、ATV `86ba6f5`、PiliPlus `f1b79ee`；迁移历史见各 finding | 2026-07-27 | 不是服务端稳定承诺；只证明多个当前客户端选择 | 多源印证 |
| API-F002 | 当前独立 signer 共同使用 nav `wbi_img`、mixin、排序、过滤、`wts` 与 `w_rid` | OSS 实现 | wiliwili `88e5876`、PiliPlus `f1b79ee`、BiliKit 当前实现 | 2026-07-27 | 无 Bilibili 官方稳定契约；已关闭社区文档不计当前证据 | 多源印证 |
| API-F003 | 当前无设备 Cookie 的游客搜索曾成功一次 | 当前行为 | `M2-guest-api-2026-07-21.md` | 2026-07-21 | 当前 OSS 常带匿名设备 Cookie；具体拒绝 code 未确认；不证明稳定 | 单次观察 |
| API-F004 | 当前最小 QR path/status 曾完成扫码、恢复和登出 | 当前行为 | M3 认证与历史验证记录 | 2026-07-21 | 两个当前 OSS 均采用更宽匿名设备请求；不能证明额外字段必要 | 单次观察 |
| API-F005 | 拒绝 redirect 可通过 delegate completion 传 `nil` 表达 | Apple 规范 | `URLSessionTaskDelegate.willPerformHTTPRedirection` | 2026-07-27 | 只描述 URLSession 行为，不证明 endpoint 不会合法 redirect | 已确认 |
| API-F006 | Domain Cookie 本身不能约束手写 Cookie header 的目标 endpoint | 标准 | RFC 6265 domain/path/secure 语义 | 2026-07-27 | 应用仍需独立 host/path allowlist | 已确认 |
| MP-F001 | RFC 8216 只强制 variant `BANDWIDTH`；Apple 设备 authoring profile 另要求分辨率、帧率、平均带宽和多视频 variant | Apple 规范＋标准 | Apple HLS Authoring Specification；RFC 8216 §4.3.4.2 | 2026-07-27 | 是否以完整 Apple profile 为产品目标仍需明确 | 已确认 |
| MP-F002 | HTTP Range 语义只约束同一 selected representation，不证明两个 CDN URL 字节身份相同 | 标准 | RFC 9110 §14；ISO BMFF 对同文件布局的 offset 语义 | 2026-07-27 | 真实候选不一致发生率未知 | 已确认 |
| MP-F003 | 合法但不可满足的 range 应采用 416；malformed、suffix、multi-range 需分别制定策略 | 标准 | RFC 9110 §14.2、§15.5.17 | 2026-07-27 | `Content-Range: bytes */length` 是 SHOULD；suffix 本身合法 | 已确认 |
| MP-F004 | 关闭 AVPlayer 自动等待会在 buffer 耗尽时暂停；当前实现没有对应恢复状态机 | Apple SDK interface＋当前源码 | Xcode 26.6 `AVPlayer.h`；`AVPlayerEngine.swift`、`AVPlayerTimelineAdapter.swift` | 2026-07-27 | 设计后果已确认，真实弱网触发频率未知 | 已确认 |
| MP-F005 | protobuf 零字节是合法 wire message，但远端弹幕 HTTP 2xx 空 body 的业务语义未知 | 标准 | Protocol Buffers Encoding Guide | 2026-07-27 | 不能由 wire 事实推导 endpoint transport policy | 已确认／边界未决 |
| MP-F006 | 四个当前 OSS 均把点播 quality 120 视作 4K，且请求高画质时使用 `fourk=1`；这不证明服务端对特定身份实际返回该档 | OSS 实现 | wiliwili `88e5876`、ATV `86ba6f5`、PiliPlus `f1b79ee`、cilicili `6f02857` | 2026-07-27 | B 站无第三方稳定契约；需当前脱敏现场请求核对内容、身份、endpoint 与 codec | 多源印证／服务行为未决 |
| MP-F007 | 一个公开 4K 样本的匿名高画质请求把 120 列入能力目录，但实际未返回 quality 120 DASH representation | 当前行为 | 一次 view＋两次非 WBI playurl 脱敏最小请求 | 2026-07-27 | 单样本、匿名、单次；不证明登录/会员态或 WBI endpoint，目录项也不等于可交付媒体 | 单次观察 |
| MP-F008 | 一个公开匿名样本的 AVC/AAC SIDX 均为完整 v0 单 box、61 个 direct SAP type 1 reference、EPT 0，总索引时长相差约 64 ms | 当前行为 | 两次 endpoint-declared index range 脱敏结构请求 | 2026-07-27 | 单样本且只覆盖当前匿名低画质；不证明 v1/large-size/额外 box 或其他边界形态不存在 | 单次观察 |
| MP-F009 | 单 AVPlayer 精确 item 替换在固定 150 ms Range 延迟下产生约 0.63 s 首帧空档；双 player 可后台准备 decoded frame，但 surface/audio 交接未证 | Apple SDK＋当前行为 | Xcode 26.6 `AVPlayer.h`；`LoopbackPlaybackServerTests.runtimeResolutionCapAndExactQualityReplacementTradeoffs` 五次对照 | 2026-07-27 | synthetic 低分辨率、无真实窗口和扬声器；不能外推真实 CDN/4K 或宣称用户无感 | 可重复观察／最终呈现未决 |
| MP-F010 | 当前 B 站 Web 普通 DASH 画质切换使用单一 video/dash player 的 fast representation switch；whole-source 切换另以旧帧 canvas 遮罩到 `canplay` | 当前一方实现 | 匿名公开页面 DOM；B 站 `core.a846b5dd.js` | 2026-07-27 | 混淆压缩脚本和单次匿名观察，不是稳定公开 API；浏览器 MSE 能力也不能直接映射为 AVPlayer 能力 | 一方当前行为／实现 |
| MP-F011 | 三个活跃 OSS 均未以真正双 surface/audio 完成手动画质交接：cilicili 采用同 item bitrate 或 warm＋snapshot＋新 player，ATV 新建 AVPlayer 后直接替换，wiliwili 以 mpv loadfile replace 重载 | OSS 实现 | cilicili `6f02857`、ATV `86ba6f5`、wiliwili `88e5876` | 2026-07-27 | 证明当前工程选择，不证明双 player 不可行或体验优劣 | 多源对照 |
| MP-F012 | AVPlayer 能在同一 HLS item 内自动切换 multivariant stream；公开播放控制只提供 peak bitrate／maximum resolution 等偏好上限，不提供精确在线 variant selector | Apple SDK／规范 | Xcode 26.6 `AVPlayerItem.h`、`AVAssetVariant.h`、`AVMetrics.h`；Apple HLS documentation | 2026-07-27 | `AVAssetVariantQualifier` 的精确 variant 能力用于下载配置；variant switch metrics 只能观察，不能发起切换 | 已确认 |
| MP-F013 | 同一 AVPlayerItem 在 synthetic 双分辨率 HLS 中曾从 132 kbps 自动降至 82 kbps，连续 6/6 通过；后续 CI 在 macOS 15 未发生降档、macOS 26 通过 | 当前行为 | opt-in `LoopbackPlaybackServerTests.automaticABRDowngradesWithinOnePlayerItem`；CI run `30427865650` | 2026-07-29 | transport 对高档施加 representation-specific 延迟，不是均匀网络模型；AVPlayer 的切换时机不是跨系统确定性契约，不再作为阻塞 Gate | 跨版本不稳定观察／恢复未决 |
| MP-F014 | 一个匿名真实样本返回两个 AVC 档位；两档 SIDX 均为 466 个 direct SAP reference、EPT 对齐，总时长差和最大累计边界差均小于 1 ms | 当前行为＋Apple 规范 | 签名脱敏 Range probe；Apple HLS Authoring Specification 7.4、8.1、8.7 | 2026-07-27 | 单样本且仅 quality 16/32；未验证真实 multivariant 播放、4K、准确 HLS peak bitrate 或其他内容形态 | 单次观察／结构兼容 |
| MP-F015 | 同一匿名真实样本的 quality 16/32 可组成 AVPlayer 识别的双 variant item；初始高档在统一总吞吐限制下自动降档且 item identity 不变，恢复到高档声明带宽 4 倍后 45 秒内未升档 | 当前行为 | 签名 test-only multivariant playback probe | 2026-07-27 | 单样本；master 仍缺真实尺寸/帧率，API bandwidth 未证明为 HLS peak；AVPlayer 升档算法与时机无公开控制契约 | 单次真实降档／恢复未决 |
| MP-F016 | 同一真实样本补入 playurl 返回的两档 width/height/frameRate 后，AVAssetVariant 识别对应视频属性；高档启动、统一限速降档与恢复 45 秒未升档的结果不变 | 当前行为 | 签名 test-only multivariant playback probe | 2026-07-27 | 只排除尺寸/帧率是该样本恢复升档的缺失开关；API bandwidth 仍未证明为 HLS peak，单样本与 45 秒不能否决 AVPlayer 后续升档 | 单次对照／恢复未决 |
| MP-F017 | 当前 loopback remote route 会在 A 对后一 Range 返回 503 时，于同一 offset 接受同长度但内容不同的 B，并对两段都返回 206 | 确定性本地行为 | `LoopbackPlaybackServerTests.remoteResourceDoesNotMixCandidatesAcrossRanges` | 2026-07-27 | synthetic A/B，不证明真实 CDN 不一致发生率；足以证明当前逐 Range fallback 允许跨对象拼接 | 可重复 reproduction／known issue |
| MP-F018 | loopback 对完整 GET、closed/open Range 与 HEAD 返回自洽；suffix、不可满足、multi-range 和未知 unit 均落入 502，不可满足缺少 `Content-Range: bytes */length` | 确定性本地行为＋RFC | `LoopbackPlaybackServerTests.loopbackRangeAndHeadResponseMatrix`；RFC 9110 §14.2、§15.5.17 | 2026-07-27 | AVPlayer 现场尚未发送这些边界 Range；不影响 server 不能把 client Range 语义统一伪装成 upstream 502 的判断 | 可重复矩阵／12 个 known issues |
| MP-F019 | 相同可恢复 Range starvation 下，AVPlayer 默认进入 waiting 并在释放后自动继续；关闭 automatic waiting 后约 0.91 秒进入 paused，释放后五秒内位置不变 | 确定性 AVPlayer 运行行为 | `LoopbackPlaybackServerTests.automaticWaitingRecoversFromRangeStarvation`；Xcode 26.6/macOS 26 | 2026-07-27 | synthetic 四秒低分辨率媒体、单机 loopback，不证明真实 CDN 发生率或首帧体验 | 可重复对照／2 个 known issues |
| MP-F020 | synthetic Range 503 后的 native failure 时机跨运行不稳定；当前 engine 对 documented failed-to-end 通知会按 item identity 收敛为一次失败事件 | 当前 AVPlayer 行为＋确定性本地机制 | opt-in `nativePlayerFailureTimingAfterRangeErrors`；`engineDeduplicatesFailureNotificationAfterReady`；失败 run `30431468445`、通过 run `30432618397` | 2026-07-29 | macOS 26 CI 曾在十秒内原生失败，macOS 15 同轮未失败；真实错误时机不作 Gate，通知去重与状态出口仍作 Gate | 系统策略不稳定／两套 CI 项目契约通过 |
| MP-F021 | 恒失败 repository 下，每轮等待当前/下一段失败完成再推进 timeline；128 次 update 对同一 identity 产生 256 次请求 | 确定性本地行为 | `DanmakuSessionTests.failedSegmentsAreNotRetriedByEveryLaterTimelineUpdate` | 2026-07-27 | 不证明真实 callback 频率、错误分布或具体重试上限；stop、换 identity 和恢复路径待实施验收 | 可重复线性增长／1 个 known issue |
| MP-F022 | 一条公开高弹幕长视频的正常中段为 HTTP 200 octet-stream，但约八百条 element 中一条 Unicode trimming 后为空，修复前 decoder 因此拒绝整个分段 | 当前真实服务行为＋不落盘 wire 形状统计 | production `BiliDanmakuProbe`；`DanmakuPayloadDecoder` | 2026-07-28 | 单视频单中段；未保存 BVID、CID、URL、正文或响应；只定位到空白正文这一字段 | 可重复真实 correctness finding |
| MP-F023 | 独立实现不会因单条空正文拒绝整段：wiliwili 曾明确提交 `Ignore empty danmaku`；cilicili 对分段 protobuf trim 后逐条返回 nil；ATV 与 PiliPlus 也不把空正文升级为整段错误 | OSS 实现＋提交历史 | wiliwili `1af41ff`；cilicili `6f02857`；ATV `86ba6f5`；PiliPlus `f1b79ee` | 2026-07-28 | wiliwili 使用旧 XML；ATV/PiliPlus 可能接受空显示项，不等于显式过滤；四仓库未检出同 endpoint 公开 issue | 多源机制对照 |
| MP-F024 | 最小修复后，Unicode 空白＋有效记录只保留有效记录、全空白分段解为零事件、缺失 ID 仍失败；同一公开异常中段由 production probe 成功解码并调度 440 条事件 | 确定性本地行为＋当前真实服务行为 | `BiliDanmakuRepositoryTests`；production `BiliDanmakuProbe`；package gate | 2026-07-28 | 单视频单中段、单机单次修复后复核；未保存 BVID、CID、URL、正文或响应；其他字段容错未放宽 | 定点回归＋真实闭环 |
| MP-F025 | 同一长视频相邻分段中，第 7 段修复后成功，第 6 段连续两次 production probe 均为 `invalid-response`，第 8 段一次为 `unavailable`；签名同进程三段探针在内存断言前失败 | 当前真实服务行为＋production 路径 | `BiliDanmakuProbe`；`M501DanmakuWorkingSetProbeTests` | 2026-07-28 | 安全错误映射本身不能区分 body、wire 与字段校验阶段；未保存标识、URL、正文、响应或测试产物 | 可重复 blocker |
| MP-F026 | 第 6 段为 HTTP 200 octet-stream、107,624 字节、wire 正常解出 852 条；现有校验类别中只有 1 条 `colorRgb > 0xFFFFFF`，其他类别与资源上限均未命中 | 当前真实服务行为＋test-only 脱敏字段分类 | `M501DanmakuWorkingSetProbeTests.testRealSegmentFailureClassificationWhenExplicitlyConfigured` | 2026-07-28 | 单视频单分段；未记录具体色值、标识、URL、正文、响应或 xcresult；颜色协议语义与第 8 段错误未判断 | 字段级根因定位 |
| MP-F027 | 三个当前活跃 protobuf 客户端均不因 `color > 0xFFFFFF` 拒绝记录或整段；cilicili 与 ATV 明确只取低 24 位 RGB，PiliPlus 保留低 24 位并补不透明 alpha；普通 color 与 colorful/会员渐变为独立字段 | OSS 实现＋提交历史 | cilicili `6f02857`；ATV `b694935`、`765865a`/`bae8097`；PiliPlus `e85c8b3`；wiliwili `e8e2de2` 仅作旧 XML 旁证 | 2026-07-28 | 非公开协议，无官方字段契约；cilicili reference 为 shallow snapshot；PiliPlus 对已存在高位的 alpha 处理与低 24 位 mask 不完全等价；未获知真实记录的具体高位含义 | 多源机制对照／支持 decoder 边界取低 24 位 |
| MP-F028 | decoder 边界取低 24 位后，宽色值定点事件被保留且基础 RGB 正确；同一真实第 6 段由 production probe 成功解码并调度 843 条 | 确定性本地行为＋当前真实服务行为 | `BiliDanmakuRepositoryTests.decoderNormalizesBaseColorRGBWithoutDroppingTheEvent`；production `BiliDanmakuProbe`；完整 direct SwiftPM suite | 2026-07-28 | 单视频单段；未保存具体色值、标识、URL、正文或响应；标准 package gate 的 `xcrun swift test` 在当前本机工具链组合下另因找不到 `Testing` 失败，等价直接调用 222 tests 通过并保留 20 个既有 known issues | 最小修复真实闭环 |
| MP-F029 | MP-014 修复后签名三段探针仍在内存断言前失败；第 8 段单独 production probe 再次返回安全错误 `unavailable` | 当前真实服务行为 | `M501DanmakuWorkingSetProbeTests`；production `BiliDanmakuProbe` | 2026-07-28 | 安全映射未暴露具体 HTTP 状态；未保存标识、URL、正文、响应或 xcresult；不能判断段越界、限流或其他服务错误 | 独立 blocker／待脱敏分类 |
| MP-F030 | 页面时长 2327 秒对应合法末段 7；固定请求的第 8 段位于时长之外，返回 HTTP 304、无 Content-Type、零 body；production scheduler 在已知 duration 下只请求末段 7 | 当前真实服务行为＋确定性本地行为 | 可指定段号的脱敏分类探针；`DanmakuSchedulerTests.finalSegmentDoesNotPrefetchBeyondKnownDuration` | 2026-07-28 | 单视频；不证明所有 endpoint/越界段都返回 304，也不覆盖 timeline duration 未知阶段 | 排除产品 blocker／定位 test-only probe 错误 |
| MP-F031 | 当前 session/identity 内，当前／下一段首次失败后会记忆对应 segment；随后共 128 次已消费 timeline update 不会重建请求，请求总数保持为 2；替换 identity 后可重新请求，stop 后无订阅和新请求，同 identity 重建 session 后可再次加载并恢复 ready | 确定性本地行为 | `DanmakuSessionTests` 3 项失败策略回归；Xcode 26.6/macOS 26；package gate | 2026-07-29 | synthetic repository；不证明真实错误发生率、网络恢复体验或未来是否需要显式重试入口 | 7 项定点测试＋232 项完整 package 测试通过／0 known issues |
| MP-F032 | production playurl decoder 把正整数 `width`／`height` 和整数或分数形式 `frame_rate` 映射为 `VideoRepresentationAttributes`；零分母、非有限值和非正尺寸不能进入领域模型 | 当前确定性 API／模型行为＋既有真实字段观察 | `BiliAPIClientTests.playURLMapsOnlyAVCAndAACRepresentations`、`playURLRejectsInvalidVideoFrameRate`；`PlaybackManifestTests.videoAttributesRejectInvalidPresentationValues`；package gate | 2026-07-29 | fixture 为脱敏手写响应；真实字段存在性由 MP-F016 支持，但本项不证明所有内容始终提供属性，也不证明 HLS master 已消费这些字段 | 23 项定点测试＋234 项完整 package 测试通过／0 known issues |
| STATE-F006 | 同一签名进程加载最后三个合法真实弹幕段 5–7，共 2,047 条、每段 436–847；cache 3→0，RSS 约 104.6→139.3 MiB 且 reset 后即时未下降 | 当前真实服务行为＋test-only 工作集 | `M501DanmakuWorkingSetProbeTests.testThreeRealSegmentsShareOneBoundedWorkingSetWhenExplicitlyConfigured` | 2026-07-28 | 单视频、Debug、单机；RSS 不区分仍可达对象与 allocator retained pages；未保存标识、URL、正文、响应或 xcresult | 结构释放已证／物理页回落未证 |
| STATE-F007 | 当前 WBI 匿名搜索连续两次均为 HTTP 200、`Cache-Control: no-cache`、完整传输 32,069 bytes；无 ETag、Last-Modified、条件请求或 304 | 当前真实服务行为＋签名 test-only 探针 | `M501SearchCacheProbeTests.testAnonymousSearchCacheHeadersWhenExplicitlyConfigured` | 2026-07-28 | 单公开查询、单时点、Debug、当前机器；不外推详情、图片或未来服务行为；查询、URL、正文与原始产物均未保留 | 单次双请求观察／无 URLCache 收益证据 |
| STATE-F008 | 当前 macOS 26 签名宿主中，50 个 loopback 合成封面在 AsyncImage 子树重建后全部再次请求；LazyVGrid 六个成功独立样本滚到底部请求 24–50 个，回到已 disappear/reappear 的顶部五次新增 0、一次新增 6 | 确定性本地行为 | `M501ImagePipelineProbeTests`；临时 xctestrun 显式开关后的定点 test-without-building | 2026-07-28 | 127.0.0.1、Debug、单机；未覆盖 macOS 15、真实 Tab、远端 CDN；RSS 高水位不等于泄漏；无账号、Cookie、远端 URL 或正文 | 完整重建缺复用／普通回滚不稳定 |
| STATE-F009 | OSS/业界形成分层做法：简单静态图可直接使用 AsyncImage；需要稳定复用、下采样、预取、并发合并、动画格式或容量控制时使用 Nuke/Kingfisher/SDWebImage 等成熟 pipeline；重功能客户端也可能自建，但必须自行承担预算与清理 | Apple/上游文档＋OSS 实现与提交历史 | Apple `AsyncImage`/`URLCache`；Nuke 13；Kingfisher 8；SDWebImageSwiftUI；ATV `86ba6f5`/`fdff13b`/`8f10032`；Darock `60d66761e`；cilicili `6f02857` | 2026-07-28 | 第三方采用率不是 BiliKit 的性能证据；cilicili 为 shallow snapshot；各 App workload、平台与缓存隐私边界不同 | 支持按需求升级，不支持现在立即替换 |
| STATE-F010 | Apple 新增 AsyncImage 缓存的直接动机是修复 Lazy 内容离屏再出现时重新加载；2027 系统按 HTTP headers 自动缓存 downloaded image data，Xcode 27 另开放逐请求 URLRequest/cache policy 和按 view hierarchy 注入自定义 URLSession/URLCache | Apple 官方文档＋WWDC26 | `AsyncImage` beta documentation；`asyncImageURLSession(_:)`；WWDC26 `What’s new in SwiftUI` 19:58–22:30 | 2026-07-28 | beta 可变；macOS 26.5 SDK 尚无新入口；不承诺 decoded image cache、下采样、预取或 transformed cache | 解释 API 动机／支持未来原生升级 |
| CONC-F001 | Swift Task 取消是协作式信号，旧结果仍需应用 identity/generation 隔离 | Apple／Swift 规范 | Swift `Task`、SwiftUI `task(id:)` interface | 2026-07-27 | generation 防写回不等于底层资源已释放 | 已确认 |
| CONC-F002 | AVPlayer periodic observer 必须与 remove 配对 | Apple SDK interface | Xcode 26.6 `AVPlayer.h` | 2026-07-27 | 静态配对不证明真实关窗释放 | 已确认 |
| CONC-F003 | URLSession 保留 delegate 至 invalidation；当前 transport 没有区分 owned/borrowed session | Apple SDK interface＋当前源码 | Xcode 26.6 `NSURLSession.h`；`HTTPClient.swift` | 2026-07-27 | 证明 owner 缺口，不证明已发生 leak | 已确认 |
| CONC-F004 | loopback `start()` task 被立即取消后仍成功返回，listener 保持 running；取消信号未进入启动状态机 | 确定性本地行为 | `LoopbackPlaybackServerTests.cancellingStartDoesNotLeaveListenerRunning`；真实 `NWListener` | 2026-07-27 | 测试结束显式 stop；不证明正常 owner 路径必然泄漏，也不覆盖 URLSession owner | 可重复机制验证／2 个 known issues |
| CONC-F005 | 修复 STATE-001 后，真实匿名 App 10 次播放进入/返回与 3 次关窗/新建均回到可用热门工作集，返回检查点无 TCP listener | 当前真实 App 行为 | ad-hoc Debug App；AX 树；`lsof`；macOS 26.5.2 | 2026-07-27 | 未逐 Task 计数，不证明所有仍可达 owner 释放；未保存 URL/正文/凭据 | 单机重复路径 |
| PRIV-F001 | `WhenUnlockedThisDeviceOnly` 与 Data Protection Keychain 支持当前最低系统，且不迁移到新设备 | Apple SDK interface／规范 | Xcode 26.6 `SecItem.h`；Apple Keychain documentation | 2026-07-27 | 真实签名 entitlement/ACL/删除仍未验证 | 已确认 |
| PRIV-F002 | `ephemeral` 不使用持久 Cookie/cache/credential storage；拒绝 redirect 需 delegate 显式返回 nil | Apple 规范 | URLSessionConfiguration／redirect delegate documentation | 2026-07-27 | 仅适用于实际采用该 configuration/delegate 的构造 | 已确认 |
| PRIV-F003 | loopback `127.0.0.1` 限制远端机器，但高熵 token 只是本机 bearer capability | 网络边界＋当前源码 | Network framework bind；`LoopbackPlaybackServer.swift` | 2026-07-27 | 不提供本机进程身份认证；严格 Host 是附加防线而非身份认证 | 已确认 |
| PRIV-F004 | 旧 GitHub-hosted 真实播放 job 与 probe runner 会让内容 identity 进入 CI/进程/stdout/临时产物；当前树已删除 hosted real probe，并改为 mode 600 输入、脱敏输出与全目录清理的本机显式 runner | 当前工作树静态事实＋构建测试 | `.github/workflows/ci.yml`；三个 probe executable；八个本机 runner；`LocalProbeInput.swift`；2026-07-28 app gate | 2026-07-28 | 未运行真实远端 probe；历史验证文档与 Git 历史未处理；未来输出变化仍需静态契约 | 当前执行路径已修复／历史边界另行裁决 |
| PRIV-F005 | 本地合成 HLS 的 AVPlayer access log 事件包含完整 loopback URI 路径与 server address；生产未读取或导出，换新哨兵后一次无 predicate unified-log 扫描未命中 | Apple SDK interface＋当前运行行为 | `AVPlayerItem.h`；`syntheticAVCAndAACReachReadyToPlayThroughLoopbackHLS`；2026-07-28 本机日志布尔扫描 | 2026-07-28 | 未覆盖 error log、崩溃诊断、真实 CDN 或其他系统；首次 predicate 命中为 log 工具自身假阳性 | 确认原始对象敏感／当前不导出策略保留 |
| PRIV-F006 | 修复前有效 token/path 配任意 Host 返回 200；修复后独立 nc 子进程验证正确 authority 200、错误 token/path 404、任意／缺失／重复／异常 Host 400、并发断连清零且 stop 后端口关闭 | 当前真实本机进程行为＋完整包回归 | `independentProcessEnforcesLoopbackCapabilityBoundary`；2026-07-28 package gate 224 项通过、20 项既有 known issue | 2026-07-28 | 不证明本机进程隔离或浏览器能取得 token；listener cancel 到端口拒绝连接是异步传播 | Host fail-open 已修复并回归 |
| UI-F001 | sidebarAdaptable、tabViewSidebarBottomBar、ScrollPosition 均覆盖最低 macOS 15 | Apple SDK interface | Xcode 26.6 SwiftUI interface | 2026-07-27 | availability 不证明 BiliKit 的具体交互正确 | 已确认 |
| UI-F002 | `.searchable`／`.toolbar`／`.onSubmit` 可表达最低工具链的原生 toolbar 搜索与提交；`.toolbarPrincipal` 虽标注 macOS 12+，但成员只存在于当前 Xcode 26 SDK，不存在于 Xcode 16.4/macOS 15.5 SDK | Apple SDK interface＋真实 CI 编译 | Xcode 26.6 SwiftUI interface；Xcode 16.4 CI build；run `30431468445` | 2026-07-29 | availability 表示运行系统下限，不保证旧 SDK 能编译后来新增的声明；`.toolbar` 与 principal 布局不等价 | 跨工具链反证已确认 |
| UI-F003 | Duration.TimeFormatStyle 覆盖 macOS 13+，但 m:ss/h:mm:ss 仍需选择 pattern | Apple SDK interface＋本机 locale 对照 | Foundation format style；中/日/英样本 | 2026-07-27 | 只对非负输入与列举边界实测 | 已确认 |
| UI-F004 | 搜索结果点击会调用播放副作用并留下非空 path，但条件手写 Binding 未使 Search NavigationStack 消费更新；改为每 Tab 独立 path 的局部 `@Bindable` 投影后，搜索与热门均可进入播放并由系统 Back 返回来源 | 当前真实 App＋确定性 UI fixture＋Apple SDK interface | 当前 Apple Development 签名 Debug App 脱敏往返；`testFixtureCoreKeyboardAndRecoveryPath`；test-only AX probe；`AppNavigationCoordinatorTests`；SwiftUI Observation interface | 2026-07-28 | 真实路径只验收一次；记录不含关键词、结果标题、BVID 或 URL | 根因已隔离／离线回归与真实往返通过 |
| UI-F005 | 播放页在窗口／系统导航焦点下，Escape、`onExitCommand`、`onKeyPress(.escape)` 与 Command-[ 均未弹栈；系统 Back 按钮可稳定返回 | 确定性 UI fixture＋Apple SDK interface | `UI008-acceptance`、`stack-exit`、`keypress-exit`、`command-back` xcresult；失败 AX hierarchy | 2026-07-28 | 尚未用熟练 VoiceOver／Full Keyboard Access 手动复核；不证明所有焦点位置都失败 | 稳定 XCUI reproduction／用户裁决 V1 不要求直接键盘返回 |
| UI-F006 | 显式 padding 的 Duration.TimeFormatStyle 在 0–100 小时的 12 个边界样本中，对 zh_CN、ja_JP、en_US 均与现有 m:ss／h:mm:ss 算法逐字相等；负 duration 会分歧，而搜索 parser 当前未拒绝负值 | Apple SDK interface＋当前 Xcode 运行对照＋当前调用链 | Xcode 26.6 Foundation；`GuestVideoCard`、`GuestVideoDetailView`、`WatchHistoryCardFormatting`；`SearchVideoPayload.durationSeconds` | 2026-07-28 | 未覆盖其他数字系统或未来 Foundation 版本；负值不是合法产品时长，应在 API 边界失败而非由 UI 格式化 | 正常域等价已证／实现前置 guard 已定位 |
| UI-F007 | 局部 @Bindable 对两个可写属性生成的 Binding 会写回同一源对象并各触发一次 didSet；当前 App 已用同一局部投影承载三个 NavigationStack path | Apple SDK interface＋当前 Xcode 运行对照＋当前源码／既有 UI 回归 | Xcode 26.6 SwiftUI/Observation；`AppShellView`；UI-008 回归 | 2026-07-28 | 最小 probe 不渲染真实 TabView/TextField；实现时仍需跑搜索 Return 与 Tab 切换 | 纯转发 setter 等价已证 |
| UI-F008 | 签名 macOS App 中，挂在 NavigationStack 实际内容上的 `.toolbarPrincipal` 搜索框在 1320 pt 窗口精确居中且宽 544 pt，Return 提交、Escape 清空；`.toolbar` 对照宽 327 pt 且右偏 490 pt；栈外 modifier 未生成字段，Command-F 未聚焦，AX identifier/label 为空而 prompt 存在 | Apple 官方文档＋当前真实 App XCUI/AX 行为 | Xcode 26.6、macOS 26.5.2；`native-searchable-ui-10.xcresult`；test-only fixture；成功截图 | 2026-07-28 | 未覆盖 1080 pt toolbar 压缩、Tab 往返和熟练 VoiceOver；宽度与样式为当前系统行为，不是固定契约 | 原生替换核心行为已证／非等价边界已定位 |
| STATE-F001 | Xcode 26 的 State(initialValue:) 不阻止 View init 先构造 class；Xcode 27 State macro 才消除该模式 | Apple WWDC26＋当前源码 | WWDC26 SwiftUI；`AppRootView.init` | 2026-07-27 | 未来工具链语义不能反推当前基线 | 已确认 |
| STATE-F002 | AsyncImage 在 macOS 15–26 没有项目可控 request/session cache 契约 | Apple SDK interface／WWDC26 | Xcode 26.6 SwiftUI interface；macOS27 新 API | 2026-07-27 | shared session 实际缓存仍需测量 | 已确认 |
| STATE-F003 | 同一 `NSHostingView<AppRootView>` 重新赋入第二套 environment root 后，播放请求仍进入第一套 State model，而呈现的 `playerContent` 来自第二套 environment | 确定性 AppKit/SwiftUI 行为 | `PlayerHostLifecycleProbeTests.rootReinitializationKeepsPlayerSurfaceWithRetainedModels`；Xcode 26.6/macOS 26.5.2 xcresult | 2026-07-27 | 合成 root re-init，不证明真实 WindowGroup 触发频率；足以证明当前构造允许 owner 代际脱节 | 可重复 expected failure |
| STATE-F004 | 单一 State-owned `AppWindowOwner` 同时持有 models/coordinator/player surface 后，root re-init 与 host dismantle 两项均普通通过，无 expected failure | 确定性 AppKit/SwiftUI 行为 | `state-owner-fixed-serialized.xcresult`；Xcode 26.6/macOS 26.5.2 | 2026-07-27 | 合成 `NSHostingView` 且关闭并行测试；不证明真实多窗口对象图已释放 | 2/2 可重复回归 |
| STATE-F005 | 当前匿名热门第一页声明 `Cache-Control: no-cache`，连续两次均完整传输约 11.5 KB | 当前真实服务行为 | 当前 BiliKit 匿名请求头；两次脱敏 curl 计数 | 2026-07-28 | 单 endpoint、单时点；不外推搜索、图片或未来服务行为 | 单次双请求观察 |
| AX-F001 | Inspector/AX tree/截图不能替代熟练 VoiceOver 真实任务 | Apple accessibility criteria | VoiceOver evaluation、Accessibility audit documentation | 2026-07-27 | 本轮未运行 VoiceOver | 已确认 |
| AX-F002 | Reduce Motion 建议读取系统设置，但 App 内等效/更细控制可参与产品策略 | Apple HIG／evaluation criteria | Apple Accessibility/Reduced Motion | 2026-07-27 | BiliKit 已有总开关和 mode 开关，优先级未决 | 已确认 |
| AX-F003 | 同一 synthetic HLS `AVPlayerItem` 在 legible rendition 为 `DEFAULT=NO`、未选择且已开始播放时没有 GET WebVTT 正文；选择该 option 后才发起正文 GET | Apple AVFoundation 运行行为＋确定性本地测试 | `LoopbackPlaybackServerTests.unifiedMasterExposesAdaptiveVariantsAndNativeSubtitles`；Xcode 26.6/macOS 26；2026-07-29 package gate | 2026-07-29 | 只证明 AVFoundation 的请求时机；production B 站字幕正文是 JSON，仍需安全、可取消且 identity 隔离的请求时 WebVTT 生成 route，并解决响应长度契约 | 可重复本地行为／按需方向已证 |
| PERF-F001 | RSS/Leaks/internal counters 各自不能证明所有仍可达闲置对象已释放 | Apple Instruments documentation | Gathering/Reducing memory use | 2026-07-27 | 需 Allocations、Memory Graph、端口/Task 信号组合 | 已确认 |
| PERF-F002 | 当前 App 10 次播放往返后 RSS 从播放约 261 MiB 回落至约 240 MiB，3 次关窗新建后约 209 MiB；30.925 秒 Allocations 中已识别弹幕/Task/listener 等容器为 0 persistent | 当前真实 App＋Instruments | `lifecycle-allocations-signed.trace` 脱敏统计；`ps`；`lsof` | 2026-07-27 | 单机短测且无 Memory Graph；raw trace 仅存 `/private/tmp`，不能宣称零 retain cycle | 单次 trace＋重复交互 |
| PERF-F003 | 当前树 80 events/s × 1081.004 秒合成生产链路请求 4 段；86480 条事件计数守恒，peak 140，stop 后 active/layer/root attachment/subscriber 归零，RSS 峰值约 120.8 MiB、即时回落约 82.3 MiB | 当前确定性性能行为 | `Scripts/run-m44-renderer-probe.sh 80 1081`；Apple Silicon/macOS 26.5.2 | 合成 repository/timeline，不含真实 AVPlayer/CDN；Debug 构建；单机一次 | 当前树长测 PASS |
| PERF-F004 | 当前签名 Debug App 热门／搜索多页往返的 10 秒保留窗口无 250 ms 以上 potential hang；RSS 约 69→138 MiB，空闲 30 秒与关窗 5 秒未回落 | 当前真实 App＋Instruments | 20 秒 windowed Time Profiler；`ps`；macOS 26.5.2 | 单机单次；未区分图片、视图和系统 cache；未覆盖 resize、历史、macOS 15 或 Release | 有界短样本／归因未决 |
| ARCH-F001 | SubtitleUseCase 对空 bvid、非正 cid 与空 trackID 的 6 个组合均在 repository 调用前失败 | 当前确定性 Application 行为 | `SubtitleUseCaseTests`；Xcode 26.6 | 2026-07-28 | 证明当前 guard 有行为，不证明类型永远不能重构 | 6/6 通过，保留 |
| ARCH-F002 | PlayerEngine 仍无 protocol-typed 调用者，但 concrete events 已被 MP-006 定点测试消费并定义 ready 后失败出口 | 当前调用图＋确定性 AVPlayer 测试 | `rg`；`engineDeduplicatesFailureNotificationAfterReady` | 2026-07-29 | production UI 尚未消费失败事件；真实 native failure 时机仅作 opt-in 观察 | protocol 可删／event 不再属于死代码 |
| ARCH-F003 | BiliWatchHistoryRepository 的 7 类 API error、CancellationError 与未知错误共 9 个映射 case 已冻结 | 当前确定性 adapter 行为 | `BiliWatchHistoryRepositoryTests`；Xcode 26.6 | 2026-07-28 | 当前测试使用待删除 protocol fake；实施时需改为可控 concrete client transport | 9/9 通过，删除前置已满足 |
| ENG-F001 | 基线 CI 在两个具体 arm64 image/toolchain 上通过无签名 Debug gate | 当前行为 | GitHub run 30206958978 | 2026-07-27 | macOS15真实 UI、Release、签名与 Intel 均未证明 | 已确认 |
| ENG-F002 | 完整 commit SHA 是 GitHub Action 不可变引用的唯一方式 | GitHub 平台规范 | Security hardening for GitHub Actions | 2026-07-27 | 当前 contents:read 降低权限但不消除供应链影响 | 已确认 |
| ENG-F003 | SwiftProtobuf 1.38.1 lock/SHA/license 一致 | 上游许可＋锁文件 | `55d7a1…`、Apache 2.0 + Runtime Library Exception | 2026-07-27 | 最终载体的 notice 呈现尚未知 | 已确认 |
| ENG-F004 | Apple Development 签名普通 Debug App 曾因无调用者的工程设置带上 user-selected read-only 文件权限；删除该设置与冗余 App Group 注册后，复建成品只保留 sandbox、network client/server、单一 Keychain group、Debug get-task-allow 与 runtime flag | 签名成品前后对照 | 两次 `codesign -d --entitlements -`、`codesign -dvvv`、当前工程调用搜索、2026-07-28 app gate | 2026-07-28 | 尚未检查 Release/Developer ID；Debug get-task-allow 属开发签名 | 已修复并验证 |
| ENG-F005 | 官方 `actions/checkout` remote 的 `v6` 当前指向基线已运行的 `d23441a…`，而编号 release `v6.0.2` 指向较早的 `de0fac2…`；pin 应保存已审阅代码身份，而非机械追随最大版本号 | 官方 repository 当前引用与提交历史 | `git ls-remote`；actions/checkout releases 与 commits | 2026-07-28 | 上游引用未来仍会移动；本轮未改 workflow、未重跑远端 CI | 已确认 |
| MP-F033 | production bridge 从每个 SIDX reference 的 byte range／duration 计算 rendition 峰值与平均码率，再聚合共享音频；master 写入多档 `BANDWIDTH`、`AVERAGE-BANDWIDTH`、`RESOLUTION`、`FRAME-RATE` 和 `CODECS`，production engine 用一个 `AVPlayerItem` 识别并播放两个 variant | Apple 规范＋RFC 8216＋当前确定性 AVFoundation 行为 | `HLSPlaylistBuilderTests`；`engineBuildsOneAdaptiveItemFromAllVideoRepresentations`；2026-07-29 app gate | 2026-07-29 | synthetic 低分辨率；当前 Xcode 未提供 `mediastreamvalidator`；未替代真实多内容、CDN、4K、升档与 stall 验证 | 235 项 package 测试、App build-for-testing 与 App unit tests 通过／0 known issues |
| MP-F034 | production 默认匿名 playurl 请求为 `qn=120`、`fnval=976`、`fourk=1`，Application／Feature 不再暴露固定质量参数；返回的全部 AVC 视频档位进入自动 master；即使 API client 注入授权器，playback 也不会请求授权 | 当前确定性请求、授权负向测试与调用图 | `BiliAPIClientTests.playURLMapsOnlyAVCAndAACRepresentations`；`BiliAPIClientTests.playbackNeverRequestsCredentialAuthorization`；`BiliCredentialRequestAuthorizerTests.rejectsHostsPathsMethodsUserInfoFragmentsAndPlainHTTP`；`BiliGuestRepository`；`GuestVideoUseCase` | 2026-07-29 | 请求能力不等于服务交付 4K；匿名现场 MP-F007 仍未返回 120，登录/会员与 HEVC/AV1 未验证 | V1 匿名自动请求／消费路径已实施，服务能力仍按身份待定 |
| UI-F009 | `VideoDurationFormatting` 以显式 minute-second／hour-minute-second padding 统一卡片、分 P、AX label 与历史进度；0–100 小时 12 个边界在中／日／英 locale 保持原字符串；搜索负 duration 不进入 formatter | 当前确定性 Foundation／API 行为 | `VideoDurationFormattingTests.nonnegativeDurationsPreserveProductClockFormatAcrossLocales`；`BiliAPIClientTests.searchRejectsNegativeDuration`；2026-07-29 package gate | 2026-07-29 | 未覆盖其他数字系统与未来 Foundation 版本；只适用于已在 adapter 边界保证非负的视频时长 | 237 项 package 测试通过／0 known issues |
| UI-F010 | `AppShellView` 的 Tab selection 与 search draft 使用局部 `@Bindable` 直接投影；三条 NavigationStack path 因承担播放 start/stop 副作用，使用显式 setter Binding 交给 coordinator 的唯一 reconciliation owner；字幕／弹幕 method Binding 未改 | Apple Observation 语义＋当前构建测试＋确定性导航回归 | `AppShellView`；`AppNavigationCoordinatorTests`；UI-F004／UI-F007／UI-F012；2026-07-29 app gate | 2026-07-29 | 显式 setter 是行为边界而非纯转发噪音；不据此扩大其他可写面 | App build-for-testing 与 App unit tests 通过 |
| UI-F011 | production `SearchTabRoot` 使用 `.searchable` 与 `.onSubmit(of:.search)`；Xcode 26+ 选择 `.toolbarPrincipal`，Xcode 16.4 编译期选择 `.toolbar`；自制 `NSSearchField` bridge、固定 340 pt 宽度和旧 AX identifier 仍保持删除 | Apple 原生 API＋当前源码／跨工具链构建＋既有签名行为 | `AppShellView`；UI-F002／UI-F008；失败 run `30431468445`、通过 run `30432618397` | 2026-07-29 | `.toolbar` 构建不承诺居中；既有 UI-F008 只覆盖 Xcode 26 principal 的 1320 pt probe，未覆盖 1080 pt、VoiceOver 与旧工具链产物 | Xcode 16.4／26.5 各 240 项 package 测试、App build-for-testing 与 App unit tests 通过 |
| ARCH-F004 | `BiliWatchHistoryRepository` 直持 `BiliAPIClient` 后，同 target 一对一 `BiliWatchHistoryService` 与测试 fake 均可删除；7 类 API mapping、Cancellation 和未知 transport 共 9 项仍由可控 client transport 保持原 Application error | 当前调用图＋确定性 concrete client 行为 | `BiliWatchHistoryRepositoryTests`；`AppEnvironment.live`；2026-07-29 app gate | 2026-07-29 | 只删除 BiliAPI target 内部抽象；不证明或授权删除跨 target `WatchHistoryRepository` port | 237 项 package 测试、App build-for-testing 与 App unit tests 通过／0 known issues |
| ARCH-F005 | 全仓没有 protocol-typed `PlayerEngine` 调用；删除宽 protocol 后，具体 `AVPlayerEngine` 必须分别显式遵循 `PlaybackControlling` 与 `PlaybackTimelineProviding`，否则 Package 自身可通过而 App composition 无法把它注入弹幕和字幕 timeline；承载 request/state/event 的源文件同步改名为 `PlayerTypes.swift` | 当前调用图＋真实 App 编译失败／修复后确定性 gate | `AppEnvironment`；`AVPlayerEngine`；2026-07-29 app gate | 2026-07-29 | package gate 不编译根 App target，不能单独证明跨 package composition；不授权删除／合并 event、state、具体方法或窄 ports | 237 项 package 测试、App build-for-testing 与 App unit tests 通过／遗漏根因已关闭 |
| MP-F035 | 精确授权 WBI playurl 的实验实现可以把 Cookie 限定在单一 host/path/method/query，且不传播到媒体 header；但“安全地限定发送位置”不能证明“应该发送” | 当前实验调用图＋安全边界 | 实验 diff；`BiliCredentialRequestAuthorizer` 既有精确 allowlist 设计 | 2026-07-29 | 实验代码与 allowlist 已从 production 回退；本项只保留设计教训，不再是当前行为声明 | 当前 Stage 删除／后续 Stage 重新取证 |
| MP-F036 | 当前签名 Debug 实验 App 在已登录状态下，对一个公开热门视频的精确授权 WBI playurl 返回 HTTP 412；有界匿名降级可恢复播放，但仍会先主动触发已知风控请求 | 当前真实 App／服务行为 | 签名 macOS App 可见脱敏错误分类与同路径前后对照 | 2026-07-29 | 单账号、单公开视频、单时点；未保存账号、内容标识、URL、Cookie、响应或截图；未定位 412 是 Cookie 集合、请求画像还是服务策略 | 支持整条实验路径回退，不支持以降级掩盖风险 |
| UI-F012 | 每个 NavigationStack path 的系统 pop 经过显式 coordinator setter；从热门夹具进入播放后点击系统 Back，导航回 feed 且 playback stop 探针由 playing 变为 stopped；非当前 Tab 的非空 path 被拒绝，不会留下 destination 而漏掉 startPlayback | 当前签名 macOS XCUI＋确定性 App 行为 | `testNativeBackStopsPlayback`；`AppNavigationCoordinatorTests.inactiveTabCannotPublishPlaybackDestination`；2026-07-29 app gate | 2026-07-29 | fixture playback 不输出真实声音，证明 stop owner 被调用但不替代真实 AVPlayer／扬声器复核 | App build-for-testing、App unit tests与定点 XCUI 路径通过 |
| AX-F004 | 卡片自绘 focus accent stroke 是返回后蓝框来源；移除自绘 stroke 后系统仍会恢复 Button 焦点，因此三个视频卡片入口进一步显式 `.focusable(false)`，hover/press 保留 | 用户真实行为＋当前源码／签名 macOS App 往返＋XCUI 返回后 Return 负向断言 | `PopularFeedView`、`VideoSearchView`、`WatchHistoryView`；`testNativeBackStopsPlayback`；2026-07-29 签名 Debug App | 2026-07-29 | 登录状态下鼠标打开公开视频、系统 Back 返回成功，焦点回到窗口而非原卡片；返回后 Return 不会重新打开视频。关闭键盘焦点会同时移除 Tab＋Return/Space 激活；VoiceOver 尚未验证 | 当前产品裁决为暂不提供卡片键盘导航 |
| AX-F005 | production 字幕目录就绪后保持 nil selection 且不请求正文；明确选择轨道后才请求正文；轨道正文失败后的重试保留原 selection 并发起第二次正文请求，reset、切视频及 A→B→A identity 隔离保持原语义 | 当前确定性字幕生命周期行为 | `SubtitleViewModelTests.catalogDefaultsToOffAndSelectedTrackFollowsTimeline`；`SubtitleViewModelTests.retryAfterTrackFailureRetriesTheSelectedTrack`；2026-07-29 app gate | 2026-07-29 | 当前仍是自制 overlay；不证明原生 legible 转换、样式或 VoiceOver 行为 | 默认关闭、按需请求、轨道失败重试与既有生命周期回归通过 |
| PRIV-F008 | `BiliPlaybackProbe` 不再使用默认 `URLSession.shared`；API、Range 与 loopback 准备请求使用显式 ephemeral session，关闭 Cookie storage/cache 并拒绝 redirect | 当前调用图＋Package 构建 | `BiliPlaybackProbe`；`BiliAPIProbe`／`BiliDanmakuProbe` 对照；2026-07-29 package gate | 2026-07-29 | 证明 Probe 构造边界，不证明远端服务不会返回风控或协议错误 | 诊断 Probe 与 production 同样保持匿名失败关闭 |

Apple 在线事实均以当前 SDK availability 或最低 Xcode CI 交叉核对；当前服务事实仍按
一次观察／多源印证标级，不因本表登记升级为稳定协议。
